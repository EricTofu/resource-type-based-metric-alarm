name: Preflight Metric Checks

on:
  pull_request:
    paths:
      - "stacks/projects/**/terraform.tfvars"

permissions:
  id-token: write
  contents: read

jobs:
  preflight:
    name: Check prerequisite metrics
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Get changed tfvars files
        id: changed
        uses: dorny/paths-filter@v3
        with:
          list-files: shell
          filters: |
            tfvars:
              - 'stacks/projects/**/terraform.tfvars'

      - name: Configure AWS credentials
        if: steps.changed.outputs.tfvars == 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.PREFLIGHT_READ_ROLE_ARN }}
          aws-region: us-east-1

      - name: Run EC2 memory metric check
        # !cancelled(): each check must run even if an earlier check FAILED —
        # a bare `if:` implies success(), which used to skip every later check.
        if: "!cancelled() && steps.changed.outputs.tfvars == 'true'"
        run: |
          FAILED=0
          for tfvars in ${{ steps.changed.outputs.tfvars_files }}; do
            echo "--- Checking EC2 metrics: $tfvars"
            scripts/check_ec2_mem_metric.sh --tfvars "$tfvars" || FAILED=1
          done
          exit $FAILED

      - name: Run ASG metric collection check
        # !cancelled(): each check must run even if an earlier check FAILED —
        # a bare `if:` implies success(), which used to skip every later check.
        if: "!cancelled() && steps.changed.outputs.tfvars == 'true'"
        run: |
          FAILED=0
          for tfvars in ${{ steps.changed.outputs.tfvars_files }}; do
            echo "--- Checking ASG metrics: $tfvars"
            scripts/check_asg_metrics.sh --tfvars "$tfvars" || FAILED=1
          done
          exit $FAILED

      - name: Run S3 request metrics check
        # !cancelled(): each check must run even if an earlier check FAILED —
        # a bare `if:` implies success(), which used to skip every later check.
        if: "!cancelled() && steps.changed.outputs.tfvars == 'true'"
        run: |
          FAILED=0
          for tfvars in ${{ steps.changed.outputs.tfvars_files }}; do
            echo "--- Checking S3 metrics: $tfvars"
            scripts/check_s3_metrics.sh --tfvars "$tfvars" || FAILED=1
          done
          exit $FAILED

      - name: Run JMX metric check
        # !cancelled(): each check must run even if an earlier check FAILED —
        # a bare `if:` implies success(), which used to skip every later check.
        if: "!cancelled() && steps.changed.outputs.tfvars == 'true'"
        run: |
          FAILED=0
          for tfvars in ${{ steps.changed.outputs.tfvars_files }}; do
            echo "--- Checking JMX metrics: $tfvars"
            scripts/check_jmx_metrics.sh --tfvars "$tfvars" || FAILED=1
          done
          exit $FAILED

      - name: Run ASG fleet metric check
        # !cancelled(): each check must run even if an earlier check FAILED —
        # a bare `if:` implies success(), which used to skip every later check.
        if: "!cancelled() && steps.changed.outputs.tfvars == 'true'"
        run: |
          FAILED=0
          for tfvars in ${{ steps.changed.outputs.tfvars_files }}; do
            echo "--- Checking ASG fleet metrics: $tfvars"
            scripts/check_asg_fleet_metrics.sh --tfvars "$tfvars" || FAILED=1
          done
          exit $FAILED
